/**
 * Brew & Bite - HowItWorks Component
 * Step-by-step visual ordering guide (Select Brew -> Customize -> Fast Local Delivery).
 */

function renderHowItWorks() {
  return `
    <!-- HowItWorks Component Placeholder -->
    <section class="how-it-works-component">
      <!-- To be populated with 3 simple order process cards -->
    </section>
  `;
}

if (typeof module !== 'undefined' && module.exports) {
  module.exports = { renderHowItWorks };
}
